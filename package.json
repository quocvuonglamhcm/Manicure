{
  "name": "NailSystem",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"node ./server/server.js\" \"cd client && npm start\"",
    "update-libs": "concurrently \"cd server && npm install\" \"cd client && npm install\"",
    "start-server": "node ./server/server.js",
    "start-client": "cd client && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
